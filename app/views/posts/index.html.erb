<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>
<% @posts.each do |post| %>
  <div>
    <p>
      ユーザー名：<%= link_to(post.user.name, "/users/#{post.user.id}") %>
      投稿内容：<%= link_to(post.content, "/posts/#{post.id}") %>
    </p>
  </div>
<% end %>
